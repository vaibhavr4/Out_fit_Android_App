<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/background_color"
    tools:context=".OutdoorMapActivity">

    <TextView
        android:id="@+id/sportsName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Sport"
        android:fontFamily="sans-serif"
        android:textSize="20dp"
        android:gravity="center"
        android:textColor="@color/white_greyish"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/mapImage"
        android:layout_width="wrap_content"
        android:layout_height="418dp"
        android:layout_alignBottom="@+id/sportsName"
        android:layout_gravity="center"
        android:layout_marginStart="0dp"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="0dp"
        android:layout_marginBottom="-431dp"
        android:src="@drawable/map" />

    <Button
        android:id="@+id/outdoorSportsButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/mapImage"
        android:layout_marginBottom="-70dp"
        android:text="Confirm Selected Ground"
        android:fontFamily="sans-serif"
        android:textStyle="bold" />

</RelativeLayout>